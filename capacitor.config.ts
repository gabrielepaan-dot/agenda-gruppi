import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gabrielepaan.agendagruppi',
  appName: 'Agenda Gruppi',
  webDir: 'dist',
};

export default config;
